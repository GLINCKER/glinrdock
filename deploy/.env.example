# GlinrDock Docker Compose Configuration
# Copy this file to .env and customize the values

# Domain and SSL
DOMAIN=your-domain.com
EMAIL=admin@your-domain.com

# Core GlinrDock settings
ADMIN_TOKEN=your-super-secure-admin-token-change-this
WEBHOOK_SECRET=your-webhook-hmac-secret

# CORS settings (comma-separated)
GLINRDOCK_CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Monitoring
GRAFANA_PASSWORD=secure-grafana-password

# Optional: Custom image tag
# GLINRDOCK_IMAGE_TAG=latest

# Optional: Custom bind address
# GLINRDOCK_BIND_ADDR=127.0.0.1:8080

# Optional: Debug logging
# GLINRDOCK_LOG_LEVEL=debug